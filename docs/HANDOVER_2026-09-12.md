# GHM Construction Handover — 2026-09-12

## Continuity priority

This is the current continuation handover for GHM construction. It supersedes `docs/HANDOVER_2026-09-11.md` for current continuation decisions while preserving that document as historical evidence.

## Mission

GHM is being built as ZAID Technologies' own backend platform so Zaid Connect and QuoteFlow can eventually move away from managed backend dependency without production downtime.

## Production safety — hard boundary

Zaid Connect and QuoteFlow remain on Supabase during construction. No production environment variables, DNS, credentials, routing, traffic, or production data may be changed. No product cutover is authorized.

Do not merge construction branches into `main`, force-push, change production `DATABASE_URL`, cut over products, copy Zaid Connect's Supabase schema blindly, remove legacy PostgreSQL authority/memberships without independent bootstrap authority, delete `ghm_db_user`, perform destructive provider cleanup, rotate credentials merely to seek authority, or expose credentials/secrets.

## Current repository state

Repository:

```text
zaidhqdev-sys/GHM
```

Current construction branch:

```text
construction/saved-business-resource
```

Current repository HEAD at this reconciliation:

```text
f5371ffc9ce10a5a82f3790269b1aa8fd51b51e5
```

HEAD message:

```text
qualify saved business concurrency and missing target boundary
```

## Historical Review and prior construction sections

The earlier sections of this handover preserve the historical construction record for Review and earlier GHM capabilities. Where an earlier section states an older branch, HEAD, OPEN status, or next action, the most recent CURRENT STATE OVERRIDE below is authoritative.

## Documentation reconciliation — CLOSED / PASS

Review documentation and runtime qualification were previously reconciled and closed. Review must not be reopened unless new evidence demonstrates a contract change, regression, security issue, failed qualification, or newly required capability.

## Provider/bootstrap authority — OPEN / BLOCKED FOR MUTATION

Unresolved provider-era memberships remain. Do not revoke them, delete `ghm_db_user`, or attempt authority/credential changes until independent bootstrap authority is established.

## Local working-tree preservation

The local worktree may contain valuable read-only audit artifacts. Do not use destructive cleanup merely to obtain a clean tree. Preserve intentional audit artifacts unless separately authorized.

## Known PowerShell trap

Do not use large `node -e` commands containing multiline SQL, JavaScript template literals/backticks, or complex quoting. Use temporary `.cjs`/`.mjs` files for genuinely necessary read-only probes. Temporary scripts under `scripts\` must require compiled modules using `../dist/...`.

## Production hard stop

Production remains on Supabase. No production migration, cutover, DNS, routing, credential, traffic, or production-data changes are authorized during GHM construction.

## CURRENT STATE OVERRIDE — 2026-09-16 — SAVED BUSINESS

This section is authoritative for continuation and supersedes earlier handover statements where they conflict with the current repository state.

### Saved Business construction — QUALIFIED / CLOSED / PASS

The GHM Saved Business first-slice resource is fully constructed and runtime-qualified for the current construction scope.

Canonical contract:

```text
docs/architecture/SAVED_BUSINESS_OPERATION_CONTRACT.md
```

Canonical resource implementation:

```text
src/resources/saved-business/contracts.ts
src/resources/saved-business/repository.ts
src/resources/saved-business/service.ts
src/resources/saved-business/service.test.ts
```

Canonical migration:

```text
database/migrations/20260916230000_create_saved_business.sql
```

Runtime qualification:

```text
scripts/qualify-saved-business-runtime.mjs
```

### Saved Business operation vocabulary

```text
saved_business.read
saved_business.create
saved_business.delete
```

The GHM boundary does not expose provider-specific `toggle`; Connect toggle behavior remains an application convenience represented by create/delete.

### Saved Business qualification — PASS

Qualification was executed against the dedicated construction PostgreSQL environment using the intended identity split:

- application/runtime behavior through `ghm_runtime`;
- controlled fixture creation and cleanup through `ghm_migrator` with `SET LOCAL ROLE ghm_schema_owner`.

The final qualification at commit `f5371ff` proved:

- `CLEANUP AUTHORITY PASS: ghm_db/ghm_migrator`
- `RUNTIME IDENTITY PASS: ghm_db/ghm_runtime`
- owner provenance on create
- owner list/read isolation
- cross-account read fail-closed (`null`)
- cross-account delete rejection
- inactive Business rejection
- unverified/non-approved Business rejection
- duplicate create rejection
- nonexistent Business rejection
- owner delete
- concurrent duplicate creation: exactly `1` success / `19` failures
- concurrent duplicate creation leaves exactly one persisted relationship
- concurrent qualification cleanup
- direct runtime INSERT denial
- direct runtime UPDATE denial
- direct runtime DELETE denial
- effective runtime privilege boundary: SELECT only on the table, with execute-only access to the controlled create/delete functions
- final `GHM SAVED BUSINESS RUNTIME QUALIFICATION: PASS`

The concurrent test specifically establishes that the database uniqueness invariant protects the relationship under simultaneous creation attempts rather than relying on an application-level read-before-insert race check.

### Saved Business validation — PASS

The user-supplied local validation run at `f5371ff` completed successfully:

```text
npm run build  PASS
npm run qualify:saved-business-runtime  PASS
npm test       PASS — 175/175
npm diff --check / git diff --check  PASS
```

The local branch was synchronized exactly with origin:

```text
construction/saved-business-resource...origin/construction/saved-business-resource
```

No working-tree changes remained after validation.

### Saved Business security boundary

The final design intentionally does not invent a spoofable PostgreSQL session-variable identity mechanism. Authenticated ownership remains established through the GHM application boundary:

```text
AuthContext.userId
    -> authorized transaction
    -> Saved Business service/repository
    -> controlled database operation
```

The runtime role cannot directly mutate `ghm.saved_business`. The controlled SECURITY DEFINER functions remain the database mutation mechanism, while application authorization binds the authenticated context to the account identifier supplied by the repository.

A raw `ghm_runtime` function call with an arbitrary account identifier is not treated as a newly defined authorization primitive; no unestablished database caller-identity mechanism was introduced merely for qualification.

### Saved Business closure rule

Saved Business is **CLOSED / PASS** for the current GHM construction scope.

Do not reopen, redesign, or requalify this slice unless new evidence demonstrates:

- a changed canonical product contract;
- a changed GHM contract;
- schema/security regression;
- failed qualification;
- a new required Saved Business capability; or
- a production adapter requirement that materially changes the contract.

The following remain explicitly deferred and are not implied by this closure:

- bulk save/unsave;
- folders, tags, collections;
- saved searches or saved enquiries;
- expiry/retention;
- automatic removal on Business eligibility change;
- Trust scoring/evidence;
- recommendations/ranking;
- notifications/realtime;
- analytics;
- anonymous saved state;
- public Saved Business reads;
- arbitrary table APIs;
- production migration or cutover.

## NEXT OUTSTANDING CONSTRUCTION CAPABILITY

**Opportunity Participation** is the next authorized GHM construction capability after Saved Business.

The next work must begin from the repository state and existing Opportunity Participation source/contract evidence. Do not reopen Saved Business, Review, Opportunity Core, Capability Catalogue, or other previously closed capabilities.

Before mutation, reconcile the Opportunity Participation boundary against the authoritative source evidence, including:

- canonical domain meaning;
- participant identity mapping;
- Opportunity ownership/creator relationship;
- Business participation relationship;
- participant role vocabulary;
- create/read/update/delete or lifecycle operation vocabulary actually required;
- authorization and visibility rules;
- active membership requirements;
- uniqueness and concurrency invariants;
- lifecycle/withdrawal semantics;
- validation and error boundaries;
- transaction/atomicity requirements;
- runtime least-privilege requirements;
- current GHM Opportunity and Business resource contracts;
- existing construction work already present on Opportunity Participation branches.

Do not implement a second participant model if an existing canonical GHM participation relationship already owns the concept. Reconcile the repository before choosing schema or operation changes.

## Continuation rule

For the next session:

1. Read this handover first.
2. Verify branch and HEAD.
3. Verify working-tree status.
4. Treat Saved Business as CLOSED / PASS.
5. Start the next governed work at Opportunity Participation.
6. Use the repository and authoritative source evidence as the source of truth.
7. Preserve the production hard stop.

No production migration, product adapter, shadow traffic, DNS/routing change, credential rotation, provider cleanup, or cutover is authorized by this handover.
