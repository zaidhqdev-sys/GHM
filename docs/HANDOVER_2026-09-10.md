# GHM Construction Handover — 2026-09-10

## Mission

GHM is being built as ZAID Technologies' own backend platform so Zaid Connect and QuoteFlow can eventually move away from managed backend dependency without production downtime.

## Continuity priority — Supabase dependency risk

On 2026-09-10 the Founder reported that Supabase has attempted to charge the account and that funds are not available. This creates a credible risk of service suspension and materially raises GHM's priority from strategic replacement work to business-continuity infrastructure.

This does **not** authorize an emergency production cutover. Supabase remains the current production backend and rollback/recovery provider until GHM has passed the governed qualification sequence. The immediate objective is to accelerate GHM qualification while preserving the ability to recover production safely.

Investigation findings:

- Zaid Connect and QuoteFlow production remain dependent on Supabase.
- No production environment variables, DNS, credentials, routing, traffic, or Supabase production data have been changed during this investigation.
- GHM's construction PostgreSQL database is reachable and the dedicated migration path is qualified.
- The dedicated `ghm_runtime` credential has now been provisioned through the controlled construction bootstrap path. Live application-runtime qualification has completed successfully against the dedicated runtime connection.
- Supabase production data must remain recoverable and must not be destructively modified because of billing/suspension risk.

## Investigation findings — PostgreSQL credentials and authority

The investigation reconciled the provider-managed credential surface with the SQL-created GHM role model.

### Provider-managed credentials

The Render PostgreSQL credential surface previously inspected exposes:

- `ghm_app_user` — Default
- `ghm_db_user`

It does not expose the SQL-created GHM roles as provider-managed credentials.

### SQL-created GHM roles

The construction database contains the separated roles:

- `ghm_schema_owner` — `NOLOGIN`, schema ownership authority;
- `ghm_migrator` — dedicated migration login;
- `ghm_runtime` — dedicated application runtime login.

These are PostgreSQL roles, not additional Render-managed credentials. Their credentials cannot be obtained merely by changing the username in a managed connection URL.

### Runtime credential finding and resolution

The existing `.env` `DATABASE_URL` remains the legacy application connection and resolves through `ghm_app_user` to `ghm_db_user`. It is not the dedicated `ghm_runtime` connection.

A controlled username-only substitution previously failed with PostgreSQL password authentication failure for `ghm_runtime`, establishing that the legacy managed password could not be assumed to provision the SQL-created runtime role.

The runtime credential was subsequently provisioned through the controlled construction bootstrap path. The resulting `GHM_RUNTIME_DATABASE_URL` is now used by the live Business Identity qualification harness and has been measured as `ghm_runtime` against `ghm_db`.

### Historical password-reset path

An earlier construction session contained a script intended to execute:

```sql
ALTER ROLE ghm_runtime PASSWORD <new-password>
```

through the legacy `DATABASE_URL` path. This is historical evidence only. It must **not** be treated as currently authorized until the present authority of `ghm_db_user` is independently verified. No password is recorded in the repository or handover.

### Authority separation finding

The previous role-separation investigation established that the old path was broader than the intended runtime boundary:

```text
ghm_app_user -> ghm_db_user
```

The target architecture remains:

```text
ghm_schema_owner  (NOLOGIN)
        ^
        | explicit migration SET ROLE path only
        |
ghm_migrator      (migration process only)
ghm_runtime       (application runtime only)
```

Bootstrap/provider authority is still required to clean up legacy memberships safely. No deletion of `ghm_db_user`, weakening of runtime boundaries, or assumption of unproven provider authority is authorized.

## Investigation findings — live Business Identity qualification harness

The first live Business Identity runtime qualification attempt produced several useful findings that were separated from actual database authority failures:

1. The harness initially inherited an unrelated `CORS_ORIGINS` requirement. It was corrected to isolate runtime qualification from that application configuration.
2. The construction PostgreSQL endpoint requires TLS. The harness was corrected to use TLS with `rejectUnauthorized: false` for this construction qualification path.
3. Cleanup initially attempted direct `DELETE` operations as the migrator connection and correctly received `42501 permission denied`. This was a **harness cleanup-authority defect**, not a runtime ACL defect. The local harness was corrected to clean up in a transaction using `SET LOCAL ROLE ghm_schema_owner`.
4. After those corrections, the qualification reached the intended runtime identity check and refused to proceed because the supplied `DATABASE_URL` still resolved as `ghm_app_user`/`ghm_db_user` rather than `ghm_runtime`.
5. A direct `ghm_runtime` authentication test previously failed with password authentication failure. The credential was subsequently provisioned through the controlled construction bootstrap path, after which the live qualification completed successfully against the dedicated runtime identity.

The qualification harness now explicitly injects the dedicated `runtimePool` into `PostgresBusinessIdentityRepository`. Its duplicate-slug atomic-failure probe also uses the currently persisted `updated.slug`, ensuring the probe targets the live unique constraint after the preceding update.

These two qualification-harness corrections are currently local working-tree changes and have not yet been committed/pushed.

## Production safety

Zaid Connect and QuoteFlow remain on Supabase. No production environment variables, DNS, credentials, routing, traffic, or Supabase production data may be changed during construction. Supabase remains the rollback provider until controlled migration is complete.

## Construction branch

`construction/ghm-database-reconciliation`

The branch remains separate from `main`. No product adapter, shadow qualification, or controlled cutover is authorized.

## Current qualification state

The current construction checkout is `construction/ghm-database-reconciliation` at `3f8d8530739469525bf68e75523ba75ea27d9997` (`3f8d853`). The branch is synchronized with its remote construction branch.

Latest validation:

- `npm run build` - PASS (`tsc`);
- `npm test` - PASS, 28/28;
- `npm run migrate` - PASS;
- `npm run qualify:business-identity-runtime` - PASS;
- live duplicate-slug unique-constraint qualification - PASS;
- `git diff --check` - PASS.

There is no separate `typecheck` npm script in this repository; `npm run build` is the canonical TypeScript compilation/typecheck gate.

The live Business Identity runtime qualification now passes against the dedicated `ghm_runtime` connection, with cleanup performed through the dedicated `ghm_migrator` authority.

The first-slice repository/runtime qualification gate is therefore closed. Broader database replacement, authority cleanup, recovery qualification, and eventual product migration remain open.

## Business Identity database qualification

The first Business Identity migration remains qualified for the construction PostgreSQL database. The first-slice objects are:

- `account_identity`
- `business`
- `business_membership`

The migration ledger contains the migration-ledger migration and Business Identity migration with repository-matching checksums. Dedicated migration-runner qualification is separately recorded as PASS.

The four GHM-owned tables (`account_identity`, `business`, `business_membership`, `ghm_schema_migrations`) and their identity sequences were transferred to `ghm_schema_owner`; the shared `public` schema was deliberately not transferred because legacy objects coexist there.

Runtime authority qualification remains PASS for the intended first-slice ACL boundary. Bootstrap membership cleanup, dedicated-schema/default-privilege reconciliation, TEMP privilege decision, and final removal of the old authority path remain open.

## PostgreSQL catalog evidence reconciliation

A fresh `npm run capture:postgres-catalog` was executed locally on 2026-09-10. The generated evidence was inspected without exposing credentials or row contents.

The capture identity is:

```text
current_database = ghm_db
session_user     = ghm_app_user
current_user     = ghm_db_user
current_role     = ghm_db_user
current_schema   = public
PostgreSQL      = 18.6
```

The regenerated catalog's `information_schema` table list contains only:

```text
files
password_reset_tokens
profiles
todos
users
```

This is an app-role-scoped visibility snapshot, not authoritative proof that the first-slice GHM tables are absent. The capture script queries `information_schema`, while dedicated migration qualification independently establishes the first-slice migration/ledger state under `ghm_migrator` → `ghm_schema_owner`.

The previous committed catalog snapshot contained the first-slice GHM tables and metadata. The fresh app-role capture therefore exposes a privilege/visibility boundary change and must not be treated as a destructive schema change.

The generated local JSON was deliberately not committed merely to overwrite the prior authoritative-looking snapshot. The architecture qualification sequence now records the scope limitation explicitly.

## PostgreSQL authority reconciliation

The original construction authority path was broader than the intended runtime boundary. The separated roles are:

- `ghm_schema_owner` — NOLOGIN, schema ownership authority;
- `ghm_migrator` — dedicated migration login with SET ROLE to `ghm_schema_owner`;
- `ghm_runtime` — application runtime login with first-slice ACL only.

The old `ghm_app_user -> ghm_db_user` path remains unresolved pending independent bootstrap/provider authority and independently qualified replacement/recovery paths.

## Dedicated migration runner

The dedicated runner uses `GHM_MIGRATOR_DATABASE_URL` and was qualified against construction PostgreSQL with:

```text
current_user     = ghm_migrator
session_user     = ghm_migrator
current_database = ghm_db
```

`SET ROLE ghm_schema_owner` succeeded, the migration ledger was reconciled, matching checksums were confirmed, and repeat/no-op execution committed successfully.

Result: `GHM DEDICATED MIGRATION RUNNER QUALIFICATION: PASS`.

## Repository implementation and runtime qualification gate

The Business Identity repository/service implementation uses explicit parameterized SQL through the authorized transaction boundary. Business creation locks the canonical `account_identity` row with `FOR UPDATE` before checking the active-membership invariant.

The authorized transaction boundary validates `AuthContext` before PostgreSQL client checkout.

The live Business Identity runtime qualification has now closed the first-slice application qualification gate:

- dedicated runtime identity measured as `ghm_runtime`;
- dedicated cleanup authority measured as `ghm_migrator`;
- profile read - PASS;
- empty membership read - PASS;
- Business creation plus owner membership - PASS;
- managed Business read - PASS;
- public approval boundary - PASS;
- managed Business update - PASS;
- business-operator role authorization rejection - PASS;
- active-membership creation rejection - PASS;
- duplicate-slug rejection - PASS;
- duplicate-slug atomic rollback - PASS;
- concurrent Business creation serialization - PASS.

The supporting repository transaction/authz test suite remains green at 28/28. The live qualification therefore establishes the intended first-slice repository/runtime behavior without requiring any broader runtime privilege.

## Immediate priority / next governed gate

The dedicated runtime credential and first-slice Business Identity runtime qualification are now complete. The next governed work is broader database replacement qualification and authority/recovery reconciliation:

1. commit the qualified runtime qualification-harness corrections;
2. reconcile this qualification result into the governing evidence/documents;
3. continue bootstrap `ghm_db_user` membership cleanup through an independently authorized provider/bootstrap path;
4. reconcile future-object default privileges only within a dedicated GHM schema;
5. make the separate database TEMP privilege decision if runtime use requires it;
6. qualify legacy-authority removal and recovery paths before any product shadow or cutover work.

No emergency production cutover is authorized merely because the managed provider may suspend service.

## Hard stops

Do not:

- merge this construction branch into `main`;
- force-push;
- change Connect or QuoteFlow production;
- copy Connect Supabase tables into GHM;
- derive schema from `ghm.db`;
- delete `ghm_db_user` while bootstrap/legacy recovery responsibilities remain unresolved;
- add shared-`public` default privileges without dedicated-schema reconciliation;
- reset or rotate PostgreSQL role credentials without verified authority and a controlled record of the mutation;
- expose credentials;
- treat a username-only change as provisioning a SQL-created role credential;
- cut over either product.
