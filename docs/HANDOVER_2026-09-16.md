# GHM Construction Handover — 2026-09-16

## Continuity priority

This handover supersedes earlier handover status where it conflicts with the repository state recorded here. Earlier handovers remain historical evidence.

## Mission

GHM is being built as ZAID Technologies' own backend platform so Zaid Connect and QuoteFlow can eventually move away from managed backend dependency without production downtime.

## Production safety — hard boundary

Zaid Connect and QuoteFlow remain on Supabase during construction. No production environment variables, DNS, credentials, routing, traffic, or production data may be changed. No product cutover is authorized.

Do not merge construction branches into `main`, force-push, change production `DATABASE_URL`, cut over products, copy Zaid Connect's Supabase schema blindly, remove legacy PostgreSQL authority/memberships without independent bootstrap authority, delete `ghm_db_user`, perform destructive provider cleanup, rotate credentials merely to seek authority, or expose credentials/secrets.

## Repository state

Repository:

```text
zaidhqdev-sys/GHM
```

Construction branch:

```text
construction/review-aggregate-reconciliation
```

This handover was added directly to GitHub on the construction branch after the Commercial migration checksum investigation.

## Completed construction before this checkpoint

### Review — QUALIFIED / CLOSED

Review construction, runtime qualification, aggregate reconciliation, authorization/security qualification, and controlled cleanup are complete. Do not repeat Review qualification unless new evidence demonstrates regression.

### Opportunity capability requirements — QUALIFIED / CLOSED

Canonical Connect source reconciliation and GHM implementation/qualification are complete. The capability requirements boundary is implemented under the established Opportunity authorization/resource architecture.

### Opportunity participation — NOT AUTHORIZED

`ghm.opportunity_participant` has not been constructed. Current source reconciliation did not establish sufficient canonical participation semantics to authorize mutation. Do not construct participant state until identity, roles, invitation/application semantics, lifecycle/withdrawal, visibility, qualification, response/evaluation/award, concurrency/duplicates, versioning, and product UI behavior are reconciled from source.

## Commercial capability — schema constructed, operation work in progress

Canonical architecture contract:

```text
docs/architecture/COMMERCIAL_CAPABILITY_ARCHITECTURE_CONTRACT.md
```

Canonical minimum schema contract:

```text
docs/architecture/COMMERCIAL_MINIMUM_SCHEMA_CONTRACT.md
```

Commercial schema migration:

```text
database/migrations/20260915150000_create_commercial_schema.sql
```

The live construction database contains the 11 Commercial tables defined by the schema migration, together with the subsequent reconciliations recorded below.

Commercial trial operation contract:

```text
docs/architecture/COMMERCIAL_TRIAL_OPERATION_CONTRACT.md
```

Commercial trial repository/service implementation is constructed. The qualification attempt reached the database boundary but is currently blocked by the migration-runner checksum mismatch described below. No Commercial trial qualification is considered PASS until the migration integrity issue is resolved and the qualification is rerun successfully.

## Commercial reconciliation migrations already applied

The live migration ledger contains these Commercial migrations after the original schema migration:

```text
20260915193000_reconcile_commercial_payment_transaction.sql
20260915194500_reconcile_commercial_access_path_indexes.sql
20260915200000_reconcile_commercial_payment_transaction_status.sql
20260915203000_reconcile_commercial_runtime_sequence_acl.sql
20260915210000_reconcile_commercial_subscription_access_path.sql
20260915220000_reconcile_commercial_payment_transaction_provider_event_idempotency.sql
```

The runtime DML ACL reconciliation for trial activation was constructed and committed as:

```text
20260915223000_reconcile_commercial_trial_runtime_dml_acl.sql
```

Commit:

```text
1749332 — fix: grant commercial trial runtime DML
```

The migration runner could not proceed to this new migration because it detected the historical checksum mismatch first.

## CURRENT BLOCKER — Commercial migration checksum mismatch

The live ledger records:

```text
version: 20260915150000
name: create_commercial_schema
checksum: d00d73c8c1f324d24cc6d28f3917c97d0edbb8d1cacbef3841054bbc951fcd97
applied_at: 2026-09-15T15:15:39.426Z
```

The current working-tree migration produces:

```text
file: database/migrations/20260915150000_create_commercial_schema.sql
bytes: 22132
BOM: false
SHA-256: a4206ae30a8903a6123bb64bc43f161bcdf6041dc49771fd3548295d556a0928
```

The current GitHub branch contains the migration as Git blob:

```text
ef44720372c856b5a59e272e1511de0f49735bad
```

Direct remote inspection of that GitHub object produced SHA-256:

```text
e376a9b55d853d96d84c70d4d5b8a4ac0a02adb97d1188fbd8ca85d13ca4d261
```

It does not match the database ledger checksum.

### Branch investigation — COMPLETE

GitHub remote branches were inspected directly using the repository's Git remote. The Commercial migration was absent from all older remote branches inspected:

```text
construction/dedicated-ghm-schema
construction/ghm-database-reconciliation
construction/ghm-production-replacement-foundation
construction/project-schema-contract
hardening/secure-runtime-baseline
main
```

It is present only on:

```text
construction/review-aggregate-reconciliation
```

Therefore the older GitHub branches do not contain a recoverable copy of the applied Commercial migration.

### Git history investigation — COMPLETE

`git log --follow` for the original Commercial migration showed only its creation commit:

```text
bd0d13d0ec53b163b180d274fd2a56f37cf69a49
feat: construct commercial capability foundation
```

No later committed modification of that original migration exists in reachable history.

### Unreachable Git object investigation — COMPLETE

An exact SHA-256 search across unreachable local Git blobs was performed for:

```text
d00d73c8c1f324d24cc6d28f3917c97d0edbb8d1cacbef3841054bbc951fcd97
```

Result:

```text
NO EXACT SHA256 MATCH FOUND IN UNREACHABLE GIT BLOBS.
```

Reachable Git object inspection found only the current Commercial migration object for the path.

### PowerShell construction-history investigation — COMPLETE

PowerShell history was inspected around the original Commercial migration construction. It confirms the file was initially created as a header and subsequently underwent controlled edits including:

- removal of two invented subscription price constraints;
- removal of redundant indexes;
- addition of seven table-specific `updated_at` triggers;
- initial Commercial grants;
- subsequent removal of the broad runtime sequence grant.

The full insertion of the table definitions was not present as an ordinary PowerShell command in the inspected history. The exact historical byte state that produced the applied `d00d73c...` checksum was therefore not recovered from command history.

### Live database reconstruction — COMPLETE

Read-only catalog reconstruction confirmed that the live construction database contains the expected Commercial schema objects, including:

- 11 Commercial tables;
- canonical constraints and foreign keys;
- current and reconciliation indexes;
- seven table-specific `updated_at` triggers;
- Commercial ledger rows;
- provider-event/payment-transaction idempotency boundary;
- subscription access-path index;
- runtime Commercial sequence protections.

The live schema is not evidence that the original migration bytes are identical to the current repository file; it is evidence of the resulting schema state after the original migration and subsequent forward reconciliations.

### Historical status observation

The current original Commercial migration contains the contract-defined payment transaction statuses:

```text
pending
succeeded
failed
```

A later forward reconciliation migration, `20260915200000_reconcile_commercial_payment_transaction_status.sql`, also establishes the same `failed` state. This is historical evidence to retain while reconstructing the original migration state; it is not sufficient by itself to rewrite the migration ledger.

## Safety decision — DO NOT BYPASS CHECKSUM INTEGRITY

The migration runner's checksum protection is functioning as designed.

**Do not manually rewrite `ghm.ghm_schema_migrations` or otherwise bypass the checksum check.**

Do not edit the applied migration merely to make the runner pass unless exact historical evidence establishes the correct bytes. Do not perform destructive schema rollback or recreate the Commercial schema solely to eliminate the checksum mismatch.

The checksum mismatch remains an OPEN construction blocker.

## Immediate next investigation

Continue forensic reconstruction from evidence outside the already-exhausted Git branch/object/history paths. The objective is to determine the exact historical migration bytes or otherwise establish a governed, integrity-preserving reconciliation path.

No Commercial trial runtime qualification may be declared PASS until:

1. the migration runner integrity check passes;
2. the pending runtime ACL migration is applied through the canonical runner;
3. Commercial trial runtime qualification is rerun;
4. owner/administrator authorization, member denial, plan/version/effective-date resolution, atomic trial/subscription/event creation, duplicate/concurrency protection, rollback, and cleanup are evidenced.

## Continuation rule

If this session ends unexpectedly, start by reading this handover and then verify:

1. current GitHub branch/HEAD;
2. local working-tree status;
3. live migration ledger checksum for `20260915150000`;
4. current repository checksum for the same migration;
5. whether the checksum blocker has been resolved by evidence;
6. whether Commercial trial runtime qualification is still OPEN.

Do not repeat the completed branch inventory, unreachable-blob search, `git log --follow`, or PowerShell-history investigation unless new evidence changes their conclusions.

Production remains on Supabase. No production migration or cutover is authorized.
