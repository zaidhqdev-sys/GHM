# GHM Construction Handover — 2026-09-09

## Mission

GHM is being built as ZAID Technologies' own backend platform so Zaid Connect and QuoteFlow can eventually move away from managed backend dependency without production downtime.

## Production safety

Zaid Connect and QuoteFlow remain on Supabase. Do not change production environment variables, DNS, credentials, routing, or live traffic during construction. Supabase remains the rollback path until controlled migration is complete.

## Construction branch

`construction/ghm-database-reconciliation`

Latest construction commit: `f262302bc85072e3dbf6bbd6391bb696b481d474`.

## Current qualification state

The previous code gate passed at `a9d6ac0f6c275ecb7533193a2139a7f477f644ef`:

- `npm run build` — PASS
- `npm run verify:runtime` — PASS
- `npm test` — PASS, 27/27
- `git diff --check` — clean
- working tree — clean and synchronized

A subsequent contract reconciliation identified that `UpdateBusinessProfileInput` exposed Connect-era fields that do not exist in the first GHM migration. The contract has now been narrowed to the actual first-slice Business fields (`name`, `slug`) so the type boundary cannot promise schema that has not been reconciled.

The current repository implementation already rejects unsupported Business update fields and only issues an UPDATE for `name` and `slug`, so the narrowed type boundary now matches the implemented first-slice SQL.

## Business Identity SQL reconciliation

The first migration defines exactly three Business Identity tables:

- `account_identity`
- `business`
- `business_membership`

with identity-backed bigint IDs, explicit role/status checks, Business slug uniqueness, one active owner per Business, and the required foreign keys/indexes.

Repository reads match the first-slice projection:

- Account: `id, full_name, phone, avatar_ref, role, created_at, updated_at`;
- Business: `id, name, slug, verification_status, is_active, created_at, updated_at`;
- Membership: all first-slice membership identity/lifecycle/audit fields.

Membership listing is intentionally active-only in the repository because `businessContext.listMemberships` is defined as the active-membership context boundary. Explicit invalid/revoked selection remains a service-layer fail-closed test using a repository that can surface inactive state.

Business creation is atomic within the authorized transaction: it creates the Business and then the owner membership. The first-slice database schema contains the structural constraints required to prevent duplicate slugs and multiple active owners.

The repository's current Business update path is limited to `name` and `slug`, which are the only editable Business columns in the first migration. This resolves the previous documentation/type drift: later Connect profile fields require a separately reconciled migration and must not be implied by the first slice.

## Runtime privilege boundary

The runtime grant design is intentionally least-privilege and based on measured repository SQL. Runtime requires SELECT on the three tables, profile INSERT/UPDATE for the authenticated account path, Business INSERT for creation, Business UPDATE only for first-slice `name`, `slug`, and `updated_at`, membership INSERT for owner participation, and identity-sequence usage for generated IDs.

Runtime must not receive schema ownership, CREATE on the application schema, CREATEDB, CREATEROLE, TRUNCATE, TRIGGER, arbitrary sequence mutation, arbitrary function execution, or migration-ledger authority. No role mutation has been performed.

The existing `ghm_db_user` remains broader than the target boundary and is not being treated as the target runtime role.

## Database state

The live GHM PostgreSQL instance remains unchanged by the migration attempt described below. Existing legacy application tables are still present; the first Business Identity migration has **not** been applied.

The migration ledger foundation remains a standalone infrastructure migration. Migration execution is not wired into application startup and is not being run as part of ordinary runtime qualification.

## Migration runner TLS qualification

On 2026-09-09, `npm run migrate` successfully built the migration runner but PostgreSQL rejected its connection with `28000: SSL/TLS required`. The migration transaction therefore did not begin and no migration was applied.

Inspection showed the migration runner used `ssl: false` outside `NODE_ENV=production`, while the existing PostgreSQL catalog capture explicitly uses `ssl: { rejectUnauthorized: false }`. The runner has been corrected to use the same explicit TLS requirement unconditionally for the construction database connection. This is a construction-only fix; no production database or environment configuration was changed.

Fix commit: `f262302bc85072e3dbf6bbd6391bb696b481d474`.

Next verification is to pull this commit and rerun `npm run migrate`. Only if the connection succeeds should the migration runner's database transaction and ledger behavior be qualified. Do not treat a successful connection as permission to proceed with broader role/grant changes.

## Next gate

1. Pull the TLS fix commit and run `npm run migrate` against the GHM construction database.
2. Verify whether the migration ledger and first Business Identity migration apply atomically and record the expected checksums.
3. Re-capture the PostgreSQL catalog and reconcile the resulting schema against the canonical first-slice contract.
4. Positively/negatively qualify the migration runner and confirm the existing legacy catalog/authority boundary does not create unsafe collisions.
5. Only after database qualification succeeds, design dedicated PostgreSQL runtime/migration identities and grants.
6. Keep Supabase production and product traffic unchanged.

## Do not do

- Do not change Zaid Connect or QuoteFlow production.
- Do not merge to `main`.
- Do not force-push.
- Do not copy Connect Supabase tables into GHM.
- Do not derive schema from `ghm.db`.
- Do not mutate PostgreSQL roles yet.
- Do not expose credentials.
- Do not cut over Zaid Connect or QuoteFlow.
